#include<stdio.h>
int main(){
    int n;
    float a;
    char b;
    long c;
    double d;
    short int e;
    long int f;

    printf("size is:%d\n",sizeof(n));
    printf("size is:%d\n",sizeof(a));
    printf("size is:%d\n",sizeof(b));
    printf("size is:%d\n",sizeof(c));
    printf("size is:%d\n",sizeof(d));
    printf("size is:%d\n",sizeof(e));
    printf("size is:%d\n",sizeof(f));

    return 0;
}